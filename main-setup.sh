#!/bin/bash

api_token="{apiToken}"
api_url="{apiUrl}"

config_needrestart() {
}

install_packages() {
}

configure_rocket_app(){
}

complete_install(){
}

config_needrestart
install_packages
complete_install
